import { DigitValidationDirective } from './digit-validation.directive';

describe('DigitValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitValidationDirective();
    expect(directive).toBeTruthy();
  });
});
